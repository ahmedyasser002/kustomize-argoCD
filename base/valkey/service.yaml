apiVersion: v1
kind: Service
metadata:
  name: valkey
  labels:
    app: valkey
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  selector:
    app: valkey
  ports:
    - name: valkey
      port: 6379
      targetPort: 6379
