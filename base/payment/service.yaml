apiVersion: v1
kind: Service
metadata:
  name: paymentservice
  labels:
    app: paymentservice
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  selector:
    app: paymentservice
  ports:
    - name: tcp-service
      port: 8080
      targetPort: 8080