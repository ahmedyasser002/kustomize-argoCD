apiVersion: v1
kind: Service
metadata:
  name: llmservice
  labels:
    app: llmservice
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  selector:
    app: llmservice
  ports:
    - name: tcp-service
      port: 8080
      targetPort: 8080