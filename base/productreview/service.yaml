apiVersion: v1
kind: Service
metadata:
  name: productreviewservice
  labels:
    app: productreviewservice
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  selector:
    app: productreviewservice
  ports:
    - name: tcp-service
      port: 8080
      targetPort: 8080