apiVersion: v1
kind: Service
metadata:
  name: quoteservice
  labels:
    app: quoteservice
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  type: ClusterIP
  selector:
    app: quoteservice
  ports:
    - name: tcp-service
      port: 8080
      targetPort: 8080