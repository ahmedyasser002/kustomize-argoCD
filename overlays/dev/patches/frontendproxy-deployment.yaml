apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontendproxy
spec:
  template:
    spec:
      containers:
        - name: frontendproxy
          resources:
            requests:
              cpu: "25m"
              memory: "25Mi"
            limits:
              cpu: "50m"
              memory: "50Mi"
