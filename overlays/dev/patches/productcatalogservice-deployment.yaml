apiVersion: apps/v1
kind: Deployment
metadata:
  name: productcatalogservice
spec:
  template:
    spec:
      containers:
        - name: productcatalogservice
          resources:
            requests:
              cpu: "25m"
              memory: "20Mi"
            limits:
              cpu: "50m"
              memory: "40Mi"
